---
phase: 04-session-engine-tracking
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - LockedIN/Session/SessionView.swift
  - LockedIN/App/AppStore.swift
autonomous: true
requirements: [SES-01, SES-05]

must_haves:
  truths:
    - "SessionView shows a countdown derived from sessionDeadline using TimelineView(.periodic(from:by:)) — no stored counter, no Timer.publish, no Combine"
    - "The live room aggregateSignal string is displayed in a prominent label with no per-name attribution (SES-05)"
    - "Participant avatars (reusing AvatarView from Phase 2) reflect live AvatarStatus from SessionCoordinator.avatarStatuses"
    - "SessionView calls SessionCoordinator.startSession on appear using .task modifier (not onAppear) and endSession on dismiss"
    - "An 'End session early' button is always visible, labelled honestly, and triggers endSession then dismisses (safety exit per SAFE UX principle)"
  artifacts:
    - path: "LockedIN/Session/SessionView.swift"
      provides: "SessionView(coordinator: SessionCoordinator, contract: CommitmentContract, participants: [Participant]) SwiftUI view"
    - path: "LockedIN/App/AppStore.swift"
      provides: "AppStore gains sessionCoordinator: SessionCoordinator property, initialised with MockFocusControlAdapter pre-loaded with demo bot scripts"
  key_links:
    - from: "LockedIN/Session/SessionView.swift"
      to: "LockedIN/Session/SessionCoordinator.swift"
      via: "@Environment or direct init injection; reads coordinator.aggregateSignal, coordinator.avatarStatuses, coordinator.sessionDeadline"
    - from: "LockedIN/App/AppStore.swift"
      to: "LockedIN/Session/SessionCoordinator.swift"
      via: "AppStore.sessionCoordinator property, MockFocusControlAdapter initialised with SessionCoordinator.makeDemoBotScripts"
---

<objective>
Build SessionView — the live session screen with countdown, aggregate signals, and avatar status indicators — wired to SessionCoordinator from Plan 01.

Purpose: This is the user-facing half of Phase 4. TimelineView drives the countdown display from a stored Date deadline (no counter drift, survives backgrounding). The view reads only coordinator-level state — no per-participant name display mid-session. AppStore gains sessionCoordinator so the view can be reached from Phase 3's room screen.

Output: SessionView.swift + AppStore update. Phase 5 (Shield) reads sessionCoordinator.metrics to populate its interruption overlay.
</objective>

<execution_context>
@/Users/yusufsalih/Developer/LockedIN/CLAUDE.md
</execution_context>

<context>
@/Users/yusufsalih/Developer/LockedIN/.planning/ROADMAP.md
@/Users/yusufsalih/Developer/LockedIN/LockedIN/App/AppStore.swift
@/Users/yusufsalih/Developer/LockedIN/LockedIN/Design/Theme.swift
@/Users/yusufsalih/Developer/LockedIN/LockedIN/Design/MoneyLabel.swift
@/Users/yusufsalih/Developer/LockedIN/.planning/phases/04-session-engine-tracking/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SessionView with TimelineView countdown</name>
  <files>LockedIN/Session/SessionView.swift</files>
  <read_first>
    LockedIN/Design/Theme.swift — colour tokens and font styles to reuse
    LockedIN/Design/MoneyLabel.swift — MoneyLabel component for the stake display
    LockedIN/Session/SessionCoordinator.swift (from Plan 01) — aggregateSignal, sessionDeadline, avatarStatuses, isActive, participants
  </read_first>
  <action>
    Create LockedIN/Session/SessionView.swift. Import SwiftUI and Foundation.

    Define struct SessionView: View that accepts coordinator: SessionCoordinator and contract: CommitmentContract as init parameters. Declare @Environment(\.dismiss) private var dismiss.

    Body: NavigationStack containing a VStack with:
    (1) A countdown section using TimelineView(.periodic(from: coordinator.sessionDeadline ?? Date(), by: 1.0)). Inside the TimelineView, compute remainingSeconds = max(0, Int((coordinator.sessionDeadline ?? Date()).timeIntervalSinceNow)). Display MM:SS formatted remaining time in a large monospaced font. When remainingSeconds == 0 and coordinator.isActive is true, trigger endSession() via a onChange or check in the TimelineView closure — call coordinator.endSession() once using a local @State var didAutoEnd: Bool to guard against repeated calls.

    (2) An aggregate signal section: a Text(coordinator.aggregateSignal) label in a rounded card. This is the only text shown about participants mid-session — no names, no per-participant breakdown (SES-05, SAFE-02). Style with a secondary background from Theme if available, otherwise use Color(.secondarySystemBackground).

    (3) An avatar row: HStack displaying one avatar slot per coordinator.participants entry. Each slot shows the participant's displayName initial letter in a Circle, plus the AvatarStatus label below it (text only if AvatarView from Phase 2 is not yet available — use a Text representation as a safe fallback so this view compiles before Phase 2 merges). Check if AvatarView is available by referencing it conditionally: define a private func avatarCell(for participant: Participant) -> some View that returns AvatarView(appearance: .placeholder, status: coordinator.avatarStatuses[participant.id] ?? .focused, size: 48) if AvatarView exists, otherwise a VStack with Circle filled with Color.accentColor overlay Text of the initial plus a Text of the status name. The .placeholder appearance value on CharacterAppearance must be defined by Phase 2 — add a comment in the call site noting this dependency.

    (4) A MoneyLabel showing the contract's stakeMinorUnits multiplied by coordinator.participants.count — total at stake. Include the TEST MODE label via MoneyLabel (it handles the badge automatically per Phase 1 spec).

    (5) An "End session early (forfeit stake)" Button at the bottom, always visible, styled in a destructive secondary style (not red-filled, just bordered). Its action calls coordinator.endSession() then dismiss(). The label must say exactly "End session early (forfeit stake)" — honest labelling per SAFE UX (SES-05 and Phase 5 SHD-05 principle).

    Use .task modifier (not onAppear) to call startSession. The .task modifier on the view should check if coordinator.participants is empty (meaning session not yet started) and call coordinator.startSession(contract: contract, participants: contract-derived participants list, adapter: ...) — however since adapter is injected via AppStore, read it from @Environment(AppStore.self) var appStore and pass appStore.focusAdapter. This means SessionView also needs @Environment(AppStore.self) var appStore.

    Do not use Timer.publish or Combine. Do not use ObservableObject. Use @State only for the didAutoEnd guard Bool.
  </action>
  <verify>
    <automated>xcodebuild -project /Users/yusufsalih/Developer/LockedIN/LockedIN.xcodeproj -scheme LockedIN -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -10</automated>
  </verify>
  <done>Build succeeds. SessionView.swift compiles. Grep confirms: no "Timer.publish" in SessionView.swift, no "import Combine" in SessionView.swift, TimelineView appears in SessionView.swift, "End session early" label text present in SessionView.swift.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SessionCoordinator into AppStore</name>
  <files>LockedIN/App/AppStore.swift</files>
  <read_first>
    LockedIN/App/AppStore.swift — existing properties: commitmentService, focusAdapter, forfeitDestination
    LockedIN/Session/SessionCoordinator.swift (Plan 01) — SessionCoordinator init, makeDemoBotScripts static factory
  </read_first>
  <action>
    Edit LockedIN/App/AppStore.swift. Add import Observation if not already present (it is, per existing file). Add a new property: let sessionCoordinator: SessionCoordinator.

    In the init, after constructing focusAdapter, build the demo bot scripts. However, makeDemoBotScripts requires three UUIDs for Maya, Leo, and Sam. Define three static let UUIDs at the top of AppStore as: static let mayaID = UUID(uuidString: "00000000-0000-0000-0000-000000000001")!, static let leoID = UUID(uuidString: "00000000-0000-0000-0000-000000000002")!, static let samID = UUID(uuidString: "00000000-0000-0000-0000-000000000003")!. These fixed UUIDs mean the same bot identities are used across Phase 3 (Participant creation) and Phase 4 (script lookup) without passing IDs around.

    Construct a MockFocusControlAdapter with the bot scripts: let demoAdapter = MockFocusControlAdapter(scripts: SessionCoordinator.makeDemoBotScripts(maya: AppStore.mayaID, leo: AppStore.leoID, sam: AppStore.samID)). Assign self.focusAdapter = demoAdapter (replacing the default empty MockFocusControlAdapter). Assign self.sessionCoordinator = SessionCoordinator().

    Also add three static computed var properties for convenience: static var mayaParticipant, leoParticipant, samParticipant returning Participant(id: mayaID, displayName: "Maya"/"Leo"/"Sam", isBot: true). These let Phase 3's room setup create the correct Participant values that match the script UUIDs.

    Keep all existing properties unchanged. The focusAdapter property type remains FocusControlAdapter — assign the demoAdapter (a MockFocusControlAdapter) to it, which satisfies the protocol type. Do not change the init signature's external API — the default argument for focusAdapter can be removed since we now build it internally; or keep it and just override in body. Simplest: remove the external focusAdapter parameter from init (it was MockFocusControlAdapter() by default anyway) and build demoAdapter internally. Keep commitmentService parameter.

    No Combine, no @Published, no ObservableObject.
  </action>
  <verify>
    <automated>xcodebuild -project /Users/yusufsalih/Developer/LockedIN/LockedIN.xcodeproj -scheme LockedIN -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -10</automated>
  </verify>
  <done>Build succeeds. AppStore has sessionCoordinator property. AppStore.mayaID, leoID, samID are accessible. AppStore.mayaParticipant returns Participant with isBot=true. focusAdapter is a MockFocusControlAdapter pre-loaded with 3 bot scripts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SessionCoordinator → SessionView | Aggregate strings only — no per-name distraction data shown mid-session (SES-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | aggregateSignal display | mitigate | aggregateSignal computed property must never include participant display names — enforced by code review of the string interpolation in SessionCoordinator (SAFE-02, SES-05) |
| T-04-05 | Denial of Service | TimelineView auto-end | mitigate | @State var didAutoEnd guards against calling endSession() more than once when the timer hits zero |
| T-04-06 | Spoofing | Static bot UUIDs | accept | Fixed UUIDs in AppStore are intentional — they ensure Phase 3 Participant creation and Phase 4 script lookup use the same IDs deterministically |
| T-04-SC | Tampering | npm/pip/cargo | accept | No package installs — pure Swift, no new SPM dependencies |
</threat_model>

<verification>
1. `xcodebuild ... build` exits 0
2. No "Timer.publish" or "import Combine" in any Session/ or App/ file: `grep -rn "Timer\.publish\|import Combine" LockedIN/Session/ LockedIN/App/` returns 0 matches
3. "End session early" text present: `grep -n "End session early" LockedIN/Session/SessionView.swift` returns a match
4. "TimelineView" present: `grep -n "TimelineView" LockedIN/Session/SessionView.swift` returns a match
5. No participant displayName appears in aggregateSignal logic: `grep -n "displayName" LockedIN/Session/SessionCoordinator.swift` shows displayName is NOT referenced in the aggregateSignal computed var
6. AppStore.samID is defined: `grep -n "samID" LockedIN/App/AppStore.swift` returns a match
</verification>

<success_criteria>
- SessionView.swift exists, compiles, uses TimelineView for countdown from Date deadline, shows aggregateSignal with no names, shows MoneyLabel for total stake, has honest "End session early (forfeit stake)" exit button always visible
- AppStore.swift updated with sessionCoordinator property, static bot UUIDs (mayaID/leoID/samID), static bot Participant factories, and focusAdapter pre-loaded with demo scripts
- Full xcodebuild succeeds across all Phase 4 files
</success_criteria>

<output>
Create `.planning/phases/04-session-engine-tracking/04-02-SUMMARY.md` when done.
</output>
