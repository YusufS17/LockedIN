---
phase: 04-session-engine-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LockedIN/Session/ParticipantMetrics.swift
  - LockedIN/Session/ContractRules.swift
  - LockedIN/Session/SessionCoordinator.swift
autonomous: true
requirements: [SES-01, SES-02, SES-03, SES-04]

must_haves:
  truths:
    - "SessionCoordinator accumulates per-participant metrics from FocusEvent streams (focused seconds, break seconds, distraction count, override count, left early, focus percentage)"
    - "Bot scripts for Maya, Leo, Sam are embedded in SessionCoordinator with hardcoded offsets — same events, same order every run (no randomness)"
    - "ContractRules.evaluate(metrics:contract:) returns .passed for Maya/Leo and .failed for Sam, determined purely by comparing distractionCount against contract.maxDistractionSeconds (converted to count threshold)"
    - "SessionCoordinator drives AvatarStatus transitions: .focused (default) → .distracted on distractionStarted → .onBreak on breakStarted → .finished on leftSession or session end"
    - "The countdown deadline is stored as a Date computed from an accelerated durationSeconds (divide real contract seconds by demoSpeedFactor = 60) so TimelineView can derive remaining seconds without a stored counter"
  artifacts:
    - path: "LockedIN/Session/ParticipantMetrics.swift"
      provides: "ParticipantMetrics struct: focusedSeconds, breakSeconds, distractionCount, overrideCount, leftEarly, focusPercentage computed var"
    - path: "LockedIN/Session/ContractRules.swift"
      provides: "ContractRules enum with static evaluate(metrics:contract:) -> RuleVerdict; RuleVerdict enum (.passed/.failed)"
    - path: "LockedIN/Session/SessionCoordinator.swift"
      provides: "@Observable SessionCoordinator with startSession(contract:participants:adapter:), endSession(), metrics dict, avatarStatuses dict, sessionDeadline Date, isActive bool"
  key_links:
    - from: "LockedIN/Session/SessionCoordinator.swift"
      to: "LockedIN/Services/FocusControlAdapter.swift"
      via: "adapter.startMonitoring(participantID:) per participant in startSession"
    - from: "LockedIN/Session/SessionCoordinator.swift"
      to: "LockedIN/Session/ParticipantMetrics.swift"
      via: "metrics[id] mutated on each FocusEvent received"
    - from: "LockedIN/Session/ContractRules.swift"
      to: "LockedIN/Models/CommitmentContract.swift"
      via: "evaluate reads contract.maxDistractionSeconds to derive distraction threshold"
---

<objective>
Create the session engine core: ParticipantMetrics, ContractRules, and SessionCoordinator.

Purpose: This is the pure-logic backbone Phase 4. The coordinator consumes FocusEvent streams from MockFocusControlAdapter, accumulates metrics per participant, and exposes avatar status transitions. ContractRules is a pure function — no side effects, no randomness. Sam's scripted events guarantee he crosses the threshold; Maya and Leo do not.

Output: Three new Swift files in LockedIN/Session/. SessionView in Plan 02 reads SessionCoordinator directly.
</objective>

<execution_context>
@/Users/yusufsalih/Developer/LockedIN/CLAUDE.md
</execution_context>

<context>
@/Users/yusufsalih/Developer/LockedIN/.planning/ROADMAP.md
@/Users/yusufsalih/Developer/LockedIN/.planning/REQUIREMENTS.md
@/Users/yusufsalih/Developer/LockedIN/LockedIN/App/AppStore.swift
@/Users/yusufsalih/Developer/LockedIN/LockedIN/Models/FocusEvent.swift
@/Users/yusufsalih/Developer/LockedIN/LockedIN/Models/CommitmentContract.swift
@/Users/yusufsalih/Developer/LockedIN/LockedIN/Models/Participant.swift
@/Users/yusufsalih/Developer/LockedIN/LockedIN/Services/FocusControlAdapter.swift
@/Users/yusufsalih/Developer/LockedIN/LockedIN/Services/MockFocusControlAdapter.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ParticipantMetrics + ContractRules</name>
  <files>LockedIN/Session/ParticipantMetrics.swift, LockedIN/Session/ContractRules.swift</files>
  <read_first>
    LockedIN/Models/CommitmentContract.swift — durationSeconds, maxDistractionSeconds, maxBreaks fields
    LockedIN/Models/FocusEvent.swift — all five cases to understand what increments which counter
  </read_first>
  <behavior>
    - ParticipantMetrics.focusPercentage: focusedSeconds / max(durationSeconds, 1) * 100, clamped 0–100
    - ContractRules.evaluate: returns .failed when metrics.distractionCount > distractionThreshold, else .passed. Threshold derived as: contract.maxDistractionSeconds / 30 (one distraction event = 30s proxy), minimum threshold 1. This means Sam with 3 scripted distraction events vs threshold of 2 returns .failed; Maya (0 events) and Leo (1 event) return .passed.
    - RuleVerdict enum: two cases — passed, failed(reason: String)
    - No randomness anywhere — pure deterministic math on Int inputs
  </behavior>
  <action>
    Create LockedIN/Session/ParticipantMetrics.swift. Define a struct ParticipantMetrics with fields: participantID: UUID, focusedSeconds: Int, breakSeconds: Int, distractionCount: Int, overrideCount: Int, leftEarly: Bool. Add a computed var focusPercentage: Double that divides focusedSeconds by max(focusedSeconds + breakSeconds + (distractionCount * 30), 1) as Double and multiplies by 100, clamped to 0–100. Provide a static func zero(for participantID: UUID) -> ParticipantMetrics initialiser returning all-zero values. No imports beyond Foundation.

    Create LockedIN/Session/ContractRules.swift. Define an enum ContractRules (caseless, methods only). Define enum RuleVerdict: Equatable with cases passed and failed(reason: String). Add static func evaluate(metrics: ParticipantMetrics, contract: CommitmentContract) -> RuleVerdict. Derive distractionThreshold = max(1, contract.maxDistractionSeconds / 30). If metrics.leftEarly return .failed(reason: "Left session early"). If metrics.distractionCount > distractionThreshold return .failed(reason: "Exceeded distraction limit: \(metrics.distractionCount) events"). Otherwise return .passed. Pure function, no side effects, no randomness (SES-04).
  </action>
  <verify>
    <automated>xcodebuild -project /Users/yusufsalih/Developer/LockedIN/LockedIN.xcodeproj -scheme LockedIN -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds. ContractRules.evaluate with distractionCount=3 and maxDistractionSeconds=60 (threshold=2) returns .failed. With distractionCount=1 returns .passed.</done>
</task>

<task type="auto">
  <name>Task 2: SessionCoordinator with bot scripts</name>
  <files>LockedIN/Session/SessionCoordinator.swift</files>
  <read_first>
    LockedIN/Services/MockFocusControlAdapter.swift — ScriptedFocusEvent struct, scripts dict init
    LockedIN/Models/Participant.swift — Participant struct fields
    LockedIN/App/AppStore.swift — focusAdapter property type
  </read_first>
  <action>
    Create LockedIN/Session/SessionCoordinator.swift. Import Foundation and Observation.

    Mark the class @Observable final class SessionCoordinator. Store: var metrics: [UUID: ParticipantMetrics] = [:], var avatarStatuses: [UUID: AvatarStatus] = [:], var sessionDeadline: Date? = nil, var isActive: Bool = false, var participants: [Participant] = [], private var monitoringTasks: [UUID: Task<Void, Never>] = [:], private var contract: CommitmentContract? = nil. AvatarStatus is an enum defined inline in this file with cases: focused, onBreak, distracted, finished — this enum will be consumed by Phase 2's AvatarView.

    Add func startSession(contract: CommitmentContract, participants: [Participant], adapter: FocusControlAdapter). Store contract and participants. Set isActive = true. Compute sessionDeadline = Date().addingTimeInterval(Double(contract.durationSeconds) / 60.0) — this is the demo-accelerated deadline (divide real duration by demoSpeedFactor 60). Initialise metrics and avatarStatuses for each participant using ParticipantMetrics.zero(for:) and .focused status. For each participant, call adapter.startMonitoring(participantID: participant.id) and consume the returned AsyncStream in a Task stored in monitoringTasks[participant.id]. Inside the Task body, iterate the stream with for await event in stream and call handleEvent(event) on each. After the stream finishes, do not crash — just let the task exit cleanly.

    Add private func handleEvent(_ event: FocusEvent). Switch on event cases. On distractionStarted: increment metrics[id]?.distractionCount by 1, set avatarStatuses[id] = .distracted. On distractionEnded: set avatarStatuses[id] = .focused. On breakStarted: set avatarStatuses[id] = .onBreak, start accumulating break time by storing the break start timestamp in a private var breakStarts: [UUID: Date] = [:]. On breakEnded: compute elapsed = Date().timeIntervalSince(breakStarts[id] ?? Date()), add Int(elapsed) to metrics[id]?.breakSeconds, clear breakStarts[id], set avatarStatuses[id] = .focused. On leftSession: set metrics[id]?.leftEarly = true, avatarStatuses[id] = .finished.

    Add func endSession(). Set isActive = false. Cancel and remove all tasks in monitoringTasks. Set avatarStatuses for all participants to .finished.

    Add a computed var aggregateSignal: String that builds a display string per SES-05 spec. Count participants with avatarStatuses == .focused (use filter). Count total distractionCount across all metrics. Return a string like "3 still perfectly LockedIN · 2 distraction events recorded" — no participant names, aggregate only (SES-05, SAFE-02).

    Also add a static func makeDemoBotScripts(maya: UUID, leo: UUID, sam: UUID) -> [UUID: [ScriptedFocusEvent]]. Maya's script (hardcoded offsets in real seconds — the adapter itself just sleeps these, but since deadline is accelerated 60x these map to ~0.5s, ~1s, etc. in the demo): at offset 15.0 breakStarted, at 30.0 breakEnded. Leo's script: at offset 10.0 distractionStarted, at 13.0 distractionEnded. Sam's script: at offset 8.0 distractionStarted, at 10.0 distractionEnded, at 14.0 distractionStarted, at 16.0 distractionEnded, at 20.0 distractionStarted, at 22.0 distractionEnded — giving Sam 3 distraction events total, crossing the threshold of 2. These are small offsets (seconds) so the demo runs fast. All offsets are hardcoded Double literals — no randomness (SES-02).

    No Combine, no Timer.publish, no ObservableObject — only @Observable and Task (per CLAUDE.md). Thread note: @MainActor is not required because @Observable handles observation tracking; mutations from async Tasks that update @Observable properties are fine in Swift 5 mode without strict concurrency.
  </action>
  <verify>
    <automated>xcodebuild -project /Users/yusufsalih/Developer/LockedIN/LockedIN.xcodeproj -scheme LockedIN -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds. SessionCoordinator, ParticipantMetrics, ContractRules, AvatarStatus, RuleVerdict types all resolve. SessionCoordinator.makeDemoBotScripts returns a dict with scripts for 3 UUIDs. endSession cancels all tasks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FocusEvent → SessionCoordinator | Only aggregate events (no app names, URLs, messages) cross this boundary — enforced by FocusEvent enum shape (SAFE-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | FocusEvent cases | mitigate | FocusEvent carries only participantID + Date; forbidden fields documented on the enum. No new fields allowed (SAFE-02) |
| T-04-02 | Tampering | ContractRules.evaluate | mitigate | Pure static function, no stored state — inputs fully determined by ParticipantMetrics and CommitmentContract structs (value types). No randomness (SES-04) |
| T-04-03 | Spoofing | Bot script offsets | accept | Scripts are hardcoded literals in source — not configurable at runtime; outcome is deterministic and expected by the product spec |
| T-04-SC | Tampering | npm/pip/cargo | accept | No package installs in this plan — pure Swift, no SPM dependencies added |
</threat_model>

<verification>
1. `xcodebuild ... build` exits 0 with no errors or warnings on the new files
2. Grep confirms no `Double` or `Float` usage in money-adjacent code paths: `grep -rn "Double\|Float" LockedIN/Session/` shows only focusPercentage (intentional, display-only Double) and no monetary fields
3. Grep confirms no `random` or `arc4random` in Session/: `grep -rn "random\|Random" LockedIN/Session/` returns 0 matches
4. Grep confirms no Combine import: `grep -rn "import Combine" LockedIN/Session/` returns 0 matches
5. Sam's script in makeDemoBotScripts contains exactly 3 distractionStarted events (grep confirms 3 `.distractionStarted` entries for Sam's array)
</verification>

<success_criteria>
- ParticipantMetrics.swift exists with struct ParticipantMetrics, static zero initialiser, focusPercentage computed var
- ContractRules.swift exists with RuleVerdict enum and static evaluate — deterministically returns .failed for Sam's scripted 3-event scenario against maxDistractionSeconds=60
- SessionCoordinator.swift exists with @Observable, startSession/endSession, metrics dict, avatarStatuses dict, aggregateSignal computed string (no names), makeDemoBotScripts static factory
- AvatarStatus enum (focused/onBreak/distracted/finished) defined in SessionCoordinator.swift and usable by Phase 2 AvatarView
- `xcodebuild build` succeeds
</success_criteria>

<output>
Create `.planning/phases/04-session-engine-tracking/04-01-SUMMARY.md` when done.
</output>
