---
phase: 03-room-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - LockedIN/Views/HomeView.swift
  - LockedIN/App/RootView.swift
autonomous: true
requirements:
  - CTR-01
  - STK-05

must_haves:
  truths:
    - "Tapping the single '£5 Serious Lock-In' tile on HomeView navigates directly to ContractView — no configuration form in between (CTR-01)"
    - "The 18+ gate fires exactly once per app session the first time the user taps the tile; confirming it stores an in-memory flag and never shows again (STK-05)"
    - "The gate uses honest copy and a single 'I confirm I am 18+' button — no deceptive UI (SAFE ethics, D-14)"
    - "RootView replaces the Phase-1 walking skeleton with the real HomeView + NavigationStack"
  artifacts:
    - path: "LockedIN/Views/HomeView.swift"
      provides: "Home screen with £5 Serious Lock-In room tile, wallet balance row, NavigationLink to ContractView"
      exports: ["HomeView"]
    - path: "LockedIN/App/RootView.swift"
      provides: "Updated root wiring: NavigationStack → HomeView (replaces walking skeleton)"
  key_links:
    - from: "HomeView room tile"
      to: "ContractView(roomStore:)"
      via: "NavigationLink / .navigationDestination"
    - from: "HomeView"
      to: "AppStore.currentBalance(for:)"
      via: ".task{} async wallet balance load"
---

<objective>
Replace the Phase-1 walking-skeleton RootView with the real app entry: a HomeView showing the single preset room tile and wallet balance, a one-shot in-memory 18+ gate sheet, and NavigationStack routing to the ContractView built in Plan 01.

Purpose: Without this, the user has no entry point into the commitment ritual. This plan wires the complete flow: launch → home → 18+ confirm (first time) → contract.

Output: Updated HomeView.swift, updated RootView.swift.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-room-contract/03-CONTEXT.md
@LockedIN/App/AppStore.swift
@LockedIN/App/RootView.swift
@LockedIN/Stores/RoomStore.swift
@LockedIN/Views/ContractView.swift
@LockedIN/Design/MoneyLabel.swift
@LockedIN/Design/Theme.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: HomeView — room tile, wallet balance, 18+ gate, navigation to ContractView (per D-13, D-14, CTR-01, STK-05)</name>
  <files>LockedIN/Views/HomeView.swift</files>
  <action>
    Create or replace `LockedIN/Views/HomeView.swift`.

    Read AppStore via `@Environment(AppStore.self) private var appStore`.

    State properties:
    - `@State private var walletBalancePence: Pence = MockCommitmentService.startingBalance` — loaded async in .task{}
    - `@State private var showAgeGate: Bool = false` — controls the 18+ sheet
    - `@State private var ageConfirmed: Bool = false` — in-memory only; no UserDefaults (D-14)
    - `@State private var roomStore: RoomStore? = nil` — created when user confirms age or re-taps after confirming; passed to ContractView

    Navigation: embed in a `NavigationStack`. Use `.navigationDestination(item: $roomStore)` — when `roomStore` becomes non-nil, push `ContractView(roomStore: roomStore)`. `RoomStore` must conform to `Identifiable` (add `var id: UUID { contract.id }` in RoomStore.swift, or use a wrapper — simpler to conform RoomStore to Identifiable directly).

    Layout (ZStack with background colour, ScrollView inner):

    **Greeting header**: "Hey, [appStore.displayName] 👋" (TypeScale.title, textPrimary) — if AppStore has no `displayName` property yet from Phase 2, fall back to a hardcoded "Hey 👋". Add `var displayName: String = "there"` to AppStore if the property is absent (check before adding).

    **Wallet row**: horizontal card showing "Wallet" label left, `MoneyLabel(walletBalancePence, compact: true)` right. Load balance in `.task { walletBalancePence = await appStore.currentBalance(for: appStore.userParticipantID) }` — if `userParticipantID` does not yet exist on AppStore, use a stable local UUID constant `let homeUserID = UUID(uuidString: "00000000-0000-0000-0000-000000000001")!` and call `currentBalance(for: homeUserID)`. Add `var userParticipantID: UUID` to AppStore if Phase 2 already added it; otherwise add a stable constant there.

    **Room tile** (D-13 — the single preset, no list or config form):
    Card styled with Theme.Colour.surfaceMid, corner radius Theme.Radius.lg. Contents:
    - Row 1: "£5 Serious Lock-In" (TypeScale.headline, textPrimary) + `MoneyLabel(500, compact: true)` trailing
    - Row 2: "25 min · 3 bots · British Red Cross" (TypeScale.caption, textSecondary)
    - Row 3: "Study hard. Keep your £5." (TypeScale.callout, accent)
    Full card is tappable (`.onTapGesture`).

    Tap action logic:
    ```
    if ageConfirmed {
        roomStore = RoomStore(appStore: appStore)
    } else {
        showAgeGate = true
    }
    ```

    **18+ gate sheet** (D-14 — honest, minimal, one-tap per STK-05):
    `.sheet(isPresented: $showAgeGate)` presenting a VStack:
    - Lock icon `Image(systemName: "lock.shield.fill")` in accent at 48pt
    - Title: "18+ Confirmation" (TypeScale.title)
    - Body: "LockedIN involves real-feeling financial stakes. You must be 18 or over to enter a cash-stake room." (TypeScale.body, textSecondary, multilineTextAlignment: .center)
    - Primary button: "I confirm I am 18+" — on tap: set `ageConfirmed = true`, dismiss sheet (`showAgeGate = false`), then `roomStore = RoomStore(appStore: appStore)` (navigates immediately after confirming)
    - Secondary text button: "Cancel" — dismisses sheet without confirming (honest exit always present per CLAUDE.md safety)
    No deceptive patterns. Both actions clearly labelled.

    Do NOT use ObservableObject, @Published, or Combine. Theme tokens only for styling.
  </action>
  <verify>
    <automated>xcodebuild -project /Users/yusufsalih/Developer/LockedIN/LockedIN.xcodeproj -scheme LockedIN -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E '(BUILD SUCCEEDED|error:)'</automated>
  </verify>
  <done>BUILD SUCCEEDED. HomeView.swift compiles. Room tile is tappable. showAgeGate sheet contains "I confirm I am 18+" button and "Cancel" button. navigationDestination(item:) pushes ContractView when roomStore is set.</done>
</task>

<task type="auto">
  <name>Task 2: RootView — replace walking skeleton with HomeView (per D-13, CTR-01)</name>
  <files>LockedIN/App/RootView.swift</files>
  <action>
    Replace the body of `RootView` with a single `HomeView()` call, preserving the `.environment` injection at the WindowGroup level (already wired in the app entry point — do not change LockedINApp.swift).

    The existing RootView struct declaration, `@Environment(AppStore.self)` property, and `#Preview` block should be retained — just swap out the `body` computed property to return `HomeView()` directly.

    Remove all walking-skeleton content (headerSection, walletSection, stakeSection, forfeitSection, the surfaceCard helper, NavigationStack wrapper, toolbarColorScheme, and the `@State private var displayBalancePence` property). HomeView owns its own NavigationStack.

    The `#Preview` block should inject a fresh `AppStore()` into `HomeView()` rather than `RootView()` if the preview was showing RootView directly — update it to `HomeView().environment(AppStore())`.

    Keep the `import SwiftUI` statement. Do not add any new imports.
  </action>
  <verify>
    <automated>xcodebuild -project /Users/yusufsalih/Developer/LockedIN/LockedIN.xcodeproj -scheme LockedIN -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E '(BUILD SUCCEEDED|error:)'</automated>
  </verify>
  <done>BUILD SUCCEEDED. RootView.swift body returns HomeView(). No walking-skeleton code remains. grep for "Walking Skeleton" in RootView.swift returns no matches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User tap → 18+ gate | Age confirmation is purely in-memory; no PII collected or stored (D-14, SAFE ethics) |
| HomeView → CommitmentService | Balance lookup goes through AppStore.currentBalance, never touches concrete service directly (SAFE-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Elevation of Privilege | 18+ gate bypass | accept | In-memory flag only; no real money moves; gate is a demo UX intent marker, not a legal KYC gate (D-14 explicitly scopes it as minimal for demo) |
| T-03-06 | Spoofing | userParticipantID as stable UUID constant | accept | Local-only prototype; no authentication surface; stable UUID is deterministic for wallet seeding only |
| T-03-07 | Information Disclosure | Wallet balance displayed on HomeView | mitigate | MoneyLabel(compact:true) enforces TEST marker alongside every £ figure — no £ rendered without it (SAFE-01/02/03) |
</threat_model>

<verification>
1. `xcodebuild -project LockedIN.xcodeproj -scheme LockedIN -destination 'platform=iOS Simulator,name=iPhone 16' build` → BUILD SUCCEEDED
2. `grep "Walking Skeleton" LockedIN/App/RootView.swift` → no matches (skeleton removed)
3. `grep "showAgeGate" LockedIN/Views/HomeView.swift` → present
4. `grep "ageConfirmed" LockedIN/Views/HomeView.swift` → present
5. `grep "18+" LockedIN/Views/HomeView.swift` → present (gate copy)
6. `grep "Cancel" LockedIN/Views/HomeView.swift` → present (honest exit button)
7. `grep -r "ObservableObject\|@Published\|import Combine" LockedIN/Views/HomeView.swift` → no matches
8. `grep "MoneyLabel" LockedIN/Views/HomeView.swift` → present (wallet row + tile stake)
</verification>

<success_criteria>
- App launches to HomeView (not the walking-skeleton RootView) — BUILD SUCCEEDED confirms entry point change
- Room tile tap shows 18+ gate sheet on first tap; sheet has honest labelled buttons (STK-05, D-14)
- Confirming age dismisses sheet and navigates to ContractView (CTR-01)
- Subsequent taps skip the gate and go directly to ContractView
- Wallet balance row shows MoneyLabel with TEST marker (SAFE-01)
</success_criteria>

<output>
Create `.planning/phases/03-room-contract/03-02-SUMMARY.md` when done.
</output>
