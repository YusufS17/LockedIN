---
phase: 02-character-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LockedIN/Design/Theme.swift
  - LockedIN/Models/CharacterAppearance.swift
  - LockedIN/Models/AvatarStatus.swift
  - LockedIN/Views/Components/AvatarView.swift
  - LockedIN/Views/Components/IsometricRoomView.swift
autonomous: true
requirements: [ONB-02, ONB-03, ONB-05]
must_haves:
  truths:
    - "A reusable AvatarView renders a CharacterAppearance and updates live when the appearance changes (foundation for ONB-02 live preview)"
    - "AvatarView supports all six AvatarStatus states with non-colour-only cues (icon + label + colour), with only .idle used this phase (ONB-03)"
    - "A reusable cozy isometric study-room view (IsometricRoomView) renders the warm dark-academia room with no external assets (ONB-05)"
    - "Theme exposes the avatar/room palette and the two-weight typography rule (Regular + Bold) the rest of the phase depends on"
  artifacts:
    - path: "LockedIN/Models/CharacterAppearance.swift"
      provides: "CharacterAppearance Codable/Equatable model + 5 option enums + .default + bot constants + VoiceOver description"
      contains: "struct CharacterAppearance"
    - path: "LockedIN/Models/AvatarStatus.swift"
      provides: "AvatarStatus enum with label + symbolName (non-colour cues, ONB-03)"
      contains: "enum AvatarStatus"
    - path: "LockedIN/Views/Components/AvatarView.swift"
      provides: "AvatarView(appearance:status:size:) — single reusable code-drawn avatar renderer (D-21a)"
      contains: "struct AvatarView"
    - path: "LockedIN/Views/Components/IsometricRoomView.swift"
      provides: "IsometricRoomView() — static reusable cozy isometric room (D-28, ONB-05)"
      contains: "struct IsometricRoomView"
    - path: "LockedIN/Design/Theme.swift"
      provides: "Skin/hair/accent/room colour tokens + headline/captionBold weight bump to .bold"
      contains: "skinLight"
  key_links:
    - from: "LockedIN/Views/Components/AvatarView.swift"
      to: "LockedIN/Models/CharacterAppearance.swift"
      via: "appearance.skinTone / hairStyle / outfitStyle / accentColour drive layer fills"
      pattern: "appearance\\.(skinTone|hairStyle|outfitStyle|accentColour)"
    - from: "LockedIN/Views/Components/AvatarView.swift"
      to: "LockedIN/Models/AvatarStatus.swift"
      via: "status overlay reads status.symbolName + status.label"
      pattern: "status\\.(symbolName|label)"
    - from: "LockedIN/Views/Components/IsometricRoomView.swift"
      to: "LockedIN/Design/Theme.swift"
      via: "wall/floor/furniture fills use Theme.Colour tokens"
      pattern: "Theme\\.Colour\\.(surface|surfaceMid|background|plantGreen|windowSlate)"
---

<objective>
Build the rendering foundation slice: the reusable `AvatarView` (code-drawn, D-21a) and the static cozy `IsometricRoomView` (D-28), backed by the `CharacterAppearance` model and `AvatarStatus` state system, plus the Theme token + typography additions every later Phase 2 screen needs.

This is the MVP foundation slice: it ships FIRST because the creator slice (02-02), the onboarding/payoff/home slice (02-03), and Phases 3/4/7 all consume `AvatarView` + `IsometricRoomView`. After this plan, every avatar/room visual in the phase is buildable; both components are visible/inspectable via SwiftUI Previews so the slice is concretely runnable, not just scaffolding.

Purpose: establish the single reusable avatar + room rendering API so later slices wire against fixed contracts instead of exploring. (D-21, D-21a, D-22, D-23, D-28; ONB-02 model, ONB-03, ONB-05)
Output: CharacterAppearance.swift, AvatarStatus.swift, AvatarView.swift, IsometricRoomView.swift (new); Theme.swift (extended).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-character-onboarding/02-RESEARCH.md
@.planning/phases/02-character-onboarding/02-PATTERNS.md
@.planning/phases/02-character-onboarding/02-UI-SPEC.md
@.planning/phases/02-character-onboarding/02-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan introduces these new symbols/files (later plans + phases consume them):
- `CharacterAppearance` (struct, Codable, Equatable) + nested enums `SkinTone`, `HairStyle`, `HairColour`, `OutfitStyle`, `AccentColour` (all `String, Codable, CaseIterable` with `displayName`) — file `LockedIN/Models/CharacterAppearance.swift`
- `CharacterAppearance.default`, `.maya`, `.leo`, `.sam` static constants; `CharacterAppearance.description` (VoiceOver) extension
- `SkinTone.colour`, `HairColour.colour`, `AccentColour.colour` Color-mapping extensions
- `AvatarStatus` (enum) with `label: String` + `symbolName: String?` + `ringColour: Color` — file `LockedIN/Models/AvatarStatus.swift`
- `AvatarView` (View) with public signature `AvatarView(appearance: CharacterAppearance, status: AvatarStatus, size: CGFloat = 80)` + private layer sub-views `AvatarBodyLayer`, `AvatarHairLayer`, `AvatarOutfitLayer`, `AvatarStatusOverlay` — file `LockedIN/Views/Components/AvatarView.swift`
- `IsometricRoomView` (View, no params) + private `LeftWallShape`, `RightWallShape`, `FloorShape` (Shape), `RoomFurnitureLayer` (View) — file `LockedIN/Views/Components/IsometricRoomView.swift`
- New `Theme.Colour` tokens: `skinLight/skinMedium/skinDark/skinDeep`, `hairBlonde/hairBrown/hairBlack/hairSilver`, `accentTeal/accentRose/accentLavender`, `plantGreen`, `windowSlate`
- Modified `Theme.TypeScale`: `headline` and `captionBold` weight changed `.semibold` → `.bold`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend Theme + add CharacterAppearance and AvatarStatus models</name>
  <files>LockedIN/Design/Theme.swift, LockedIN/Models/CharacterAppearance.swift, LockedIN/Models/AvatarStatus.swift</files>
  <read_first>
    - LockedIN/Design/Theme.swift (file being modified — copy the existing `Color(red:green:blue:)` token pattern and TypeScale pattern exactly)
    - LockedIN/Models/Money.swift and LockedIN/Models/Participant.swift (analogs: Foundation-only model files, doc-header pattern — per 02-PATTERNS.md §CharacterAppearance / §AvatarStatus)
    - .planning/phases/02-character-onboarding/02-PATTERNS.md §"LockedIN/Design/Theme.swift", §"CharacterAppearance.swift", §"AvatarStatus.swift" (exact token names, enum cases, code excerpts)
    - .planning/phases/02-character-onboarding/02-UI-SPEC.md §Color, §Typography (token hex values; the two-weight Regular+Bold rule; the explicit `headline`/`captionBold` → `.bold` update instruction; note `title2` and `callout` must stay untouched)
  </read_first>
  <action>
    Modify `LockedIN/Design/Theme.swift`: inside `enum Colour`, append the avatar/room tokens from 02-PATTERNS.md (skin: skinLight/skinMedium/skinDark/skinDeep; hair: hairBlonde/hairBrown/hairBlack/hairSilver; accent variants: accentTeal/accentRose/accentLavender; room: plantGreen, windowSlate) using the existing `Color(red:green:blue:)` one-token-per-line style with a hex comment. Per the UI-SPEC two-weight rule, change ONLY `TypeScale.headline` and `TypeScale.captionBold` from `weight: .semibold` to `weight: .bold`; leave `title2` (.semibold) and `callout` (.medium) untouched (they are unused in Phase 2).

    Create `LockedIN/Models/CharacterAppearance.swift` (Foundation only): `struct CharacterAppearance: Codable, Equatable` with vars `skinTone: SkinTone`, `hairStyle: HairStyle`, `hairColour: HairColour`, `outfitStyle: OutfitStyle`, `accentColour: AccentColour`. Add the five nested option enums as `String, Codable, CaseIterable` each with a `displayName` computed property — cases per 02-PATTERNS.md (SkinTone: light/medium/dark/deep; HairStyle: short/curly/long/tied; HairColour: blonde/brown/black/silver; OutfitStyle: casual/academic/hoodie/smart; AccentColour: amber/teal/rose/lavender). Add static constants `.default`, `.maya`, `.leo`, `.sam` (values per 02-PATTERNS.md, addressing RESEARCH Q3 seeded bot appearances). Add a `var description: String` extension producing the VoiceOver string required by UI-SPEC §Accessibility ("<Skin> skin, <HairStyle> <HairColour> hair, <Outfit> outfit"). Add Color-mapping extensions `SkinTone.colour`, `HairColour.colour`, `AccentColour.colour` mapping each case to the matching `Theme.Colour` token (amber maps to `Theme.Colour.accent`); these need `import SwiftUI` — put them either at the bottom of this file (add `import SwiftUI`) or in AvatarView.swift, executor's choice, but they must compile.

    Create `LockedIN/Models/AvatarStatus.swift` (Foundation only for the enum; add `import SwiftUI` if `ringColour` lives here): `enum AvatarStatus: String, Equatable` with cases idle/focused/deepFocus/onBreak/distracted/finished. Add `var label: String` and `var symbolName: String?` per 02-PATTERNS.md, and a `var ringColour: Color` mapping each non-idle state to the UI-SPEC §AvatarView status table (focused→accent, deepFocus→accentTeal, onBreak→textSecondary, distracted→forfeitRed, finished→moneyGreen; idle→clear). All six cases declared now; only `.idle` is rendered this phase (ONB-03 forward-compat).

    Do NOT use `@Published`/`ObservableObject`/Combine (CLAUDE.md). Do NOT redefine existing Theme tokens — only append/modify the named ones.
  </action>
  <verify>
    <automated>xcodebuild -project LockedIN.xcodeproj -scheme LockedIN -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5 | grep -q 'BUILD SUCCEEDED'</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild ... build` ends `** BUILD SUCCEEDED **`.
    - `grep -q 'skinLight' LockedIN/Design/Theme.swift` and `grep -q 'windowSlate' LockedIN/Design/Theme.swift` succeed.
    - `grep -E 'static let headline .*weight: .bold' LockedIN/Design/Theme.swift` and `grep -E 'static let captionBold .*weight: .bold' LockedIN/Design/Theme.swift` both match (no remaining `.semibold` on those two lines).
    - `grep -E 'static let (title2|callout)' LockedIN/Design/Theme.swift` still shows `.semibold` / `.medium` respectively (untouched).
    - `CharacterAppearance.swift` declares `struct CharacterAppearance: Codable, Equatable` and the five enums conform to `CaseIterable`; `.default`, `.maya`, `.leo`, `.sam` and `var description` all present (grep each identifier).
    - `AvatarStatus.swift` declares all six cases and both `label` and `symbolName` members (grep `case deepFocus`, `var symbolName`).
  </acceptance_criteria>
  <done>Theme palette + two-weight typography applied; CharacterAppearance and AvatarStatus models compile with all required members, defaults, bot constants, and VoiceOver description.</done>
</task>

<task type="auto">
  <name>Task 2: Build AvatarView (code-drawn, D-21a) with status overlay + previews</name>
  <files>LockedIN/Views/Components/AvatarView.swift</files>
  <read_first>
    - LockedIN/Design/MoneyLabel.swift (analog: reusable stateless component, immutable inputs, private sub-view decomposition, `#Preview` blocks, Theme tokens — per 02-PATTERNS.md §AvatarView)
    - LockedIN/Models/CharacterAppearance.swift and LockedIN/Models/AvatarStatus.swift (created in Task 1 — the inputs this view consumes)
    - .planning/phases/02-character-onboarding/02-PATTERNS.md §"AvatarView.swift" (layer order, ZStack alignment, sub-view structure, accessibility label, SkinTone.colour extension)
    - .planning/phases/02-character-onboarding/02-UI-SPEC.md §"Reusable Component Contracts → AvatarView" (public signature, layer order, the full status overlay table, badge geometry, default size 80, sizes 120/80/48/40, Reduce-Motion rule for overlays, D-21 vs D-21a note) and §Accessibility (accessibilityLabel, badge accessibilityHidden)
  </read_first>
  <action>
    Create `LockedIN/Views/Components/AvatarView.swift` (`import SwiftUI`). Public signature MUST be exactly `AvatarView(appearance: CharacterAppearance, status: AvatarStatus, size: CGFloat = 80)` — this contract is consumed by 02-02, 02-03, and Phases 3/4/7; do not change it. Compose a `ZStack(alignment: .bottomTrailing)` of code-drawn layers bottom→top per UI-SPEC: `AvatarBodyLayer` (skin tone, head circle + body rounded-rect silhouette), `AvatarHairLayer` (hair style + `hairColour.colour`), `AvatarOutfitLayer` (outfit shape tinted with `accentColour.colour`), and `AvatarStatusOverlay` shown only when `status != .idle`. Implement the four 3–4 visually-distinct option variations per layer using SwiftUI shapes (Circle/Ellipse/RoundedRectangle/Path) so each `HairStyle`/`OutfitStyle` case looks different (D-21b). Size everything as fractions of `size` so the avatar scales cleanly at 120/80/48/40.

    `AvatarStatusOverlay`: a small badge (~`size * 0.28` circle filled with `status.ringColour` at ~0.9 opacity) containing the `status.symbolName` SF Symbol — colour AND icon together (non-colour-only, ONB-03); offset by `Theme.Spacing.xs`. Mark the overlay `.accessibilityHidden(true)`. On the root `AvatarView`, set `.accessibilityElement(children: .ignore)` and `.accessibilityLabel("Avatar: \(appearance.description), status: \(status.label)")`. Status overlay appearance uses opacity only — never scale/slide (UI-SPEC overlay motion rule).

    This is the D-21a code-drawn implementation (the execution default per D-21a — not a fallback to apologise for). Keep the layer sub-views `private` so a later D-21 PNG upgrade swaps only their internals without touching the public API.

    Add `#Preview` blocks demonstrating: (a) `.default` appearance at size 120; (b) all four `AccentColour` / `OutfitStyle` variations side by side; (c) a row showing every `AvatarStatus` case (idle + the five active states) so the overlay system is visually inspectable now even though only `.idle` ships. Previews use `Theme.Colour.background`.
  </action>
  <verify>
    <automated>xcodebuild -project LockedIN.xcodeproj -scheme LockedIN -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5 | grep -q 'BUILD SUCCEEDED'</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild ... build` ends `** BUILD SUCCEEDED **`.
    - `grep -E 'AvatarView\(appearance:.*status:.*size: CGFloat = 80' LockedIN/Views/Components/AvatarView.swift` (or equivalent init) confirms the exact public signature with default size 80.
    - File contains private layer sub-views (grep `AvatarBodyLayer`, `AvatarHairLayer`, `AvatarOutfitLayer`, `AvatarStatusOverlay`).
    - Status overlay renders only for non-idle (grep `status != .idle`) and uses both `status.symbolName` and `status.ringColour` (grep both).
    - Root view sets `.accessibilityLabel(` referencing `appearance.description` and `status.label` (grep `accessibilityLabel`).
    - At least one `#Preview` block exercises multiple `AvatarStatus` cases (grep `#Preview` count ≥ 2; grep `.focused` and `.distracted` present in previews).
    - Manual (record in SUMMARY, per 02-VALIDATION.md ONB-02/ONB-03): open the AvatarView preview; confirm changing appearance changes the render and each status shows icon+colour+ (badge), not colour alone.
  </acceptance_criteria>
  <done>AvatarView renders a CharacterAppearance with code-drawn layers, exposes the fixed public API, shows non-colour-only status badges for all five active states, carries a VoiceOver label, and is inspectable via previews.</done>
</task>

<task type="auto">
  <name>Task 3: Build IsometricRoomView (static cozy room, D-28) with previews</name>
  <files>LockedIN/Views/Components/IsometricRoomView.swift</files>
  <read_first>
    - LockedIN/App/RootView.swift (analog: ZStack + `Theme.Colour.background.ignoresSafeArea()` shell, GeometryReader usage — per 02-PATTERNS.md §IsometricRoomView "closest structural analog")
    - LockedIN/Design/Theme.swift (the room/furniture tokens added in Task 1)
    - .planning/phases/02-character-onboarding/02-RESEARCH.md §"Pattern 3: Isometric Room" (Path-based wall/floor parallelogram math, 2:1 ratio, fill choices)
    - .planning/phases/02-character-onboarding/02-PATTERNS.md §"IsometricRoomView.swift" (ZStack draw order, LeftWallShape Path coordinates, GeometryReader shell)
    - .planning/phases/02-character-onboarding/02-UI-SPEC.md §"Reusable Component Contracts → IsometricRoomView" (no-params signature, geometry, fill colours, furniture table desk/bookshelf/lamp/plant/window, the ~(50%,58%) avatar desk slot, "no animation, static in Phase 2", and the Phase-4 `participants:` seam to bake-but-not-implement)
  </read_first>
  <action>
    Create `LockedIN/Views/Components/IsometricRoomView.swift` (`import SwiftUI`). Public signature MUST be parameterless: `IsometricRoomView()` (Phase 2 static room; do NOT add a `participants:` parameter now — leave a comment noting Phase 4 adds avatar placement via overlay/new param). Use a `GeometryReader` + `ZStack` shell with `Theme.Colour.background` behind everything. Draw, in back-to-front order: `RightWallShape` (shadow side, flat `Theme.Colour.surface`), `LeftWallShape` (ambient side, `LinearGradient` surfaceMid→surface topLeading→bottomTrailing), `FloorShape` (LinearGradient background→surfaceMid top→bottom) — each a `Shape` using `Path` parallelograms at the ~2:1 isometric ratio with coordinates per 02-PATTERNS.md / RESEARCH Pattern 3. Then a `RoomFurnitureLayer` view drawing the code-only furniture from the UI-SPEC table: desk (surfaceMid with surface border), bookshelf with 3–5 accent/accentSoft book-spine blocks, desk lamp (accentSoft glow circle ~40% opacity + textSecondary stand), plant (plantGreen oval leaves + surfaceMid pot), window (windowSlate panel + accentSoft glow ~0.15). No external assets, no `Image(...)` of bundled art. No animation — the room is static this phase.

    Do NOT place an `AvatarView` inside `IsometricRoomView`; callers (02-03 PayoffBeatView/HomeView) layer the avatar on top in their own ZStack at the documented (~50% width, ~58% height) desk slot. Add a doc comment recording that desk-slot CGPoint convention so callers position consistently.

    Add `#Preview` blocks: (a) full-screen room on a dark background; (b) the room with a sample `AvatarView(appearance: .default, status: .idle, size: 80)` overlaid at the (50%,58%) desk slot to demonstrate the caller-composition pattern (this previews the ONB-05 payoff composition without building PayoffBeatView yet).
  </action>
  <verify>
    <automated>xcodebuild -project LockedIN.xcodeproj -scheme LockedIN -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5 | grep -q 'BUILD SUCCEEDED'</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild ... build` ends `** BUILD SUCCEEDED **`.
    - `grep -E 'struct IsometricRoomView: View' LockedIN/Views/Components/IsometricRoomView.swift` matches and the initializer takes no parameters (no `participants` parameter present: `grep -v '^//' LockedIN/Views/Components/IsometricRoomView.swift | grep -c 'participants:'` returns 0).
    - File declares the three room shapes and the furniture layer (grep `LeftWallShape`, `RightWallShape`, `FloorShape`, `RoomFurnitureLayer`).
    - Furniture uses code-drawn primitives and the new tokens (grep `Theme.Colour.plantGreen` and `Theme.Colour.windowSlate`); no bundled-art image load (`grep -v '^//' LockedIN/Views/Components/IsometricRoomView.swift | grep -c 'Image(\"'` returns 0 — SF Symbols via `Image(systemName:)` are allowed but bundled-name images are not).
    - At least one `#Preview` composes `AvatarView` over the room (grep `AvatarView(` inside the file's preview section).
    - Manual (record in SUMMARY, per 02-VALIDATION.md ONB-05): open the IsometricRoomView preview; confirm a recognizable warm dark-academia isometric room renders with desk/shelf/lamp/plant/window and an avatar sits at the desk.
  </acceptance_criteria>
  <done>IsometricRoomView renders a static cozy isometric study room from SwiftUI Path + gradients + code-drawn furniture, no external assets, parameterless API ready for reuse, with a preview demonstrating avatar-over-room composition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new this plan) | Pure in-memory model + stateless SwiftUI rendering. No input, no persistence, no network in this plan. Persistence/input boundaries are introduced in 02-02 / 02-03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service (crash) | `AvatarView` rendering an out-of-range/missing enum case | mitigate | All appearance/status enums are exhaustive `String` enums with total `switch` mappings (no force-unwrap, no array index); CaseIterable cycling cannot produce an invalid case |
| T-02-02 | Information Disclosure | Avatar/room rendering | accept | No PII, no money, no network — appearance data is cosmetic and local-only; nothing to disclose |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan or phase (CLAUDE.md forbids SPM deps; RESEARCH §Package Legitimacy Audit: not applicable). No supply-chain surface. |
</threat_model>

<verification>
- `xcodebuild -project LockedIN.xcodeproj -scheme LockedIN -destination 'platform=iOS Simulator,name=iPhone 16' build` ends `** BUILD SUCCEEDED **` after each task.
- All new `.swift` files are auto-included via the synchronized root group (no pbxproj edits; confirm by successful build picking them up).
- Manual (per 02-VALIDATION.md): AvatarView preview shows live appearance changes + non-colour status cues (ONB-03); IsometricRoomView preview shows the cozy room with avatar at desk (ONB-05).
</verification>

<success_criteria>
- AvatarView public API `AvatarView(appearance:status:size:)` exists and renders all six AvatarStatus states with non-colour-only cues (ONB-03).
- IsometricRoomView renders a static cozy isometric room with zero external assets (ONB-05).
- CharacterAppearance model + option enums + default + bot constants exist and compile (foundation for ONB-02).
- Theme exposes the avatar/room palette and the corrected two-weight (Regular+Bold) typography.
- Build green; components inspectable via previews.
</success_criteria>

<output>
Create `.planning/phases/02-character-onboarding/02-01-SUMMARY.md` when done. Record: chosen layer-shape approach per option, the desk-slot CGPoint convention, any D-21a shape decisions, and manual preview verification notes (ONB-03 cue check, ONB-05 room look).
</output>
